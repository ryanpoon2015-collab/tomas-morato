// ============================================================
// config.js — Environment Configuration
// ============================================================

const CONFIG = {
  API_MODE: 'mock',                       // 'mock' or 'live'
  API_BASE_URL: 'http://localhost:8000',  // FastAPI / Express / Laravel server base URL
  API_VERSION: 'v1',
  AUTH_TOKEN_KEY: 'tm_owner_token',
};
